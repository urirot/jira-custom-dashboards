# Jira Custom Dashboards Environment Configuration
# Copy this file to .env and fill in your actual values

# Jira API Configuration
# Your Jira domain (e.g., yourcompany.atlassian.net)
JIRA_DOMAIN=yourcompany.atlassian.net

# Your Jira email address (the one you use to log into Jira)
JIRA_EMAIL=your.email@company.com

# Your Jira API token (NOT your password)
# Generate this at: https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=your_api_token_here

# Optional: Backend server port (defaults to 4000 if not set)
# PORT=4000

# Optional: Frontend development server port (defaults to 3000 if not set)
# REACT_APP_PORT=3000

# =============================================================================
# IMPORTANT SECURITY NOTES:
# =============================================================================
# 1. NEVER commit your actual .env file to version control
# 2. Keep your API token secure and don't share it
# 3. The .env file is already in .gitignore to prevent accidental commits
# 4. Generate a new API token if you suspect it has been compromised
#
# =============================================================================
# HOW TO GET YOUR JIRA API TOKEN:
# =============================================================================
# 1. Go to https://id.atlassian.com/manage-profile/security/api-tokens
# 2. Click "Create API token"
# 3. Give it a meaningful label (e.g., "Jira Custom Dashboards")
# 4. Copy the generated token and paste it as JIRA_API_TOKEN
#
# =============================================================================
# EXAMPLE VALUES (DO NOT USE THESE - THEY ARE JUST EXAMPLES):
# =============================================================================
# JIRA_DOMAIN=acme.atlassian.net
# JIRA_EMAIL=john.doe@acme.com
# JIRA_API_TOKEN=ATATT3xFfGF0example1234567890abcdef
